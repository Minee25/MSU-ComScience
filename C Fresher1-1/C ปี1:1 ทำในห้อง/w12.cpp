#include <stdio.h>

int main() {
    int i = 1, maxium, minium, num = 0;
    printf("Enter Number: ");
    scanf("%d", &num);
    maxium = num;
    minium = num;
    while (i<=4) {
        printf("Enter Number: ");
        scanf("%d", &num);
        if (num > maxium) {
            maxium = num;
        }
        if (
            weeee22e2wq) {
            minium = num;
        }
        i += 1;
    }
    printf("\nMax: %d\n", maxium);
    printf("Min: %d\n", minium);
}
